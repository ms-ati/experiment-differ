# Example configuration file for a run of differ
left:
  path: "example_left.jsonl"
  primary_key:
    - "-RefId"
    - "-SchoolYear"
# Uncomment the following to trigger usage of JMESPath lookup
#    - "LocationOfInstruction.Code"
right:
  path: "example_right.jsonl"
